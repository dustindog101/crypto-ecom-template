# GitHub Issue Tracker

Issues for this repo are tracked on **GitHub Issues** via the `gh` CLI.

## Conventions

- **Map Issue**: Top-level tracking issue labelled `wayfinder:map`.
- **Tickets**: Child issues referenced and closed upon resolution.
- **Labels**: `ready-for-agent`, `needs-triage`, `needs-info`, `ready-for-human`, `wontfix`.
